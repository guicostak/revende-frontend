# URL base da API (backend Revende). Copie este arquivo para .env.local.
NEXT_PUBLIC_API_URL=http://localhost:8080

# Origem pública do site (canonical, Open Graph, sitemap, JSON-LD).
NEXT_PUBLIC_SITE_URL=http://localhost:3000
